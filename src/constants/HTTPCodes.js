const OK = 200;
const CREATED = 201;
const NO_CONTENT = 204;
const NOT_FOUND = 404;
/**
 *
 * @type {{CREATED: number, NOT_FOUND: number, NO_CONTENT: number, OK: number}}
 */
module.exports = {
    OK,
    CREATED,
    NO_CONTENT,
    NOT_FOUND
}